# HR Policy RAG Assistant

This project implements a Retrieval‑Augmented Generation (RAG) chatbot for HR policies using Python, ChromaDB, and OpenAI.
Employees can ask policy questions and receive grounded answers sourced directly from official HR documents.
Run `pip install -r requirements.txt`, set `OPENAI_API_KEY`, and execute `python hr_policy_rag.py`.
``